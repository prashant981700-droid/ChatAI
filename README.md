# ChatAI
ChatGPT-style working web app.

## Run
python -m venv .venv
.venv\\Scripts\\activate
pip install -r requirements.txt
python app.py

Open http://127.0.0.1:5000

It works in DEMO AI mode without an API key. Later, an OpenAI-compatible API can be connected using .env.
